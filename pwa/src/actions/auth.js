import firebase from '../config/firebase';

import {
	REGISTER_SUCCESS,
	REGISTER_FAIL,
	USER_LOADED,
	AUTH_ERROR,
	LOGIN_SUCCESS,
	LOGIN_FAIL,
	LOGOUT,
} from './types';

// Load User

// export const loadUser = () => async (dispatch) => {
//   if (localStorage.token) {

//   }
// }

// Register User

// Login User

// Logout
